# Netlify — build via Git OU arrastar a pasta dist/ em app.netlify.com/drop
[build]
  command = "npm run build"
  publish = "dist"

# SPA fallback: todas as rotas (ex.: /passeio/trilha-do-bonete) servem o index.html
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
